This is a database stress-testing application on python.

It supposed to be adaptable to various different database engines, currently have working engines for Postgresql, MariaDB and CockroachDB.

You've implemented:
 - sqeeel/database_modules, providing running the database engine and running queries against it using docker-based images;
 - sqeeel/template_instantiator module, that allows to generate queries that are scalable in size;
 - sqeeel/query_generator module that generates query templates based on a bison-style grammar parser tuned to the database calling database module hooks;
 - sqeeel/stress_engine, that applies each given template running stress loop over whole range of sizes;
 - runner application with CLI interface;
 - some limited unit tests in tests/ directory.

Now we add an YugabyteDB adapter. The YugabyteDB has two interfaces: YCQL and YSQL, and we should make separate execution adapters for them.
Also, we should support running as 1-node and distributed, which means we should support 1-node and 3-node execution modes.

The grammar is based on Postgresql, the grammar file is uploaded to grammars/yugabyte-db-parser_gram.y
(do not read file contents as it 200kb long). Copy the grammar handling rules from current postgresql implementation.

Please propose and ask for explicit confirmation on the architecture of changes to the application and classes structure.

>>> Review answer

I've replaced grammar files:
    `grammars/yugabyte-db-ycql-parser_gram.y` -- contains grammar for YCQL
    `yugabyte-db-ysql-gram.y` -- contains grammar for YSQL.

They are different, but both based on postgresql grammar so the initial rewriter rules could be similar, but I will tweak them later to match the lexer and metadata.

The architecture proposal is good for me. Proceed with the implementation plan.

>>>>> Addendum

The YSQL implementation missing hung & client-hung query detection.
When query hung is, the bin/ysqlsh still running, and server query still running too, but it is detected as "timeout". The detection logic and recovery should be similar to postgresql one.

>>>>> Addendum

The YCQL implementation is also missing hung & client-hung query detection.
The observation shows that when query hung is, the client `python /home/yugabyte/bin/ycqlsh.py` is still running, and the result is detected as timeout.
We need a similar detection logic for YCQL, including attempt to cancel the client via SIGINT first, and cancelling server-side.

YCQL executor incorrectly handles errors and it should also force-finish all queries with semicolon.

>>>>> Addendum

Example:

```
2025-12-03 18:36:39,655 -     Full run stats: DockerExecResult(status='success', error_message=None, duration=0.38978569497703575, exit_code=0, stdout='', stderr='<stdin>:2:Incomplete statement at end of file\n')
Result: ('success', '')
```

Now we've got error from the initialization:

```
RuntimeError: Initialization query failed: CREATE TABLE IF NOT EXISTS x(x int PRIMARY KEY);
Error: <stdin>:2:InvalidRequest: Error from server: code=2200 [Invalid query] message="No Namespace Used
CREATE TABLE IF NOT EXISTS x(x int PRIMARY KEY);
                           ^
 (ql error -215)"
 ```

>>>> Addendum

No, the YCQL error handling is still wrong.

first of all, ANY stderr output means error, f.e.:

```
2025-12-03 18:55:35,538 -   Running query for size 1000 (query size: 2015 bytes)...
2025-12-03 18:55:36,436 -   Finished in 0.8980s. Effect: ('success', '')
2025-12-03 18:55:36,436 -     Full run stats: DockerExecResult(status='success', error_message=None, duration=0.8980470669921488, exit_code=0, stdout='', stderr="<stdin>:2:NoHostAvailable: ('Unable to complete the operation against any hosts', {<Host: 172.17.0.2:9042 datacenter1>: ConnectionShutdown('Connection to 172.17.0.2:9042 was closed')})\n")
```

second: the ConnectionShutdown error looks like signal of crash; at least afterwards no other queries works with:
```
2025-12-03 18:55:39,156 -     Full run stats: DockerExecResult(status='success', error_message="Connection error: ('Unable to connect to any servers', {'172.17.0.2:9042': ConnectionRefusedError(11", duration=0.38762560801114887, exit_code=1, stdout='', stderr='Connection error: (\'Unable to connect to any servers\', {\'172.17.0.2:9042\': ConnectionRefusedError(111, "Tried connecting to [(\'172.17.0.2\', 9042)]. Last error: Connection refused")})\n')
```

Please also keep to the error effect only first line of the stderr:

```
  100 - 100: ('error', '<stdin>:2:SyntaxException: <Error from server: code=2000 [Syntax error in CQL query] message="Invalid CQL Statement. Only one selected table is allowed\nselect x  from x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x;\n               ^\n (ql error -12)">')
```

should become just ('error', '<stdin>:2:SyntaxException: <Error from server: code=2000 [Syntax error in CQL query] message="Invalid CQL Statement. Only one selected table is allowed')

Like that done for other databases.

>>>>> Fixing multi-node startup

`--yb-nodes 3` does not work, it still only runs single node; the 2nd and 3rd nodes failing with error:

```
$ docker logs sqeeel-yb-ycql-2
Fetching configs from join IP...
ERROR: Node at the join ip provided is not reachable.
For more information, check the logs in /root/var/logs
```

but it doesn't detect that startup is not successful.
